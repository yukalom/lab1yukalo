{
  "name": "frontend",
  "private": true,
  "type": "module",
  "main": "src/main.ts",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p src/tsconfig.json && vite build"
  },
  "devDependencies": {
    "vite": "^8.0.8"
  }
}