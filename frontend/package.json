{
  "name": "frontend",
  "private": true,
  "type": "module",
  "main": "src/main.ts",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build"
  },
  "devDependencies": {
    "vite": "^6.0.5",
    "typescript": "^5.7.3"
  }
}