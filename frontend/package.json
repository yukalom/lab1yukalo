{
  "name": "frontend",
  "private": true,
  "type": "module",
  "main": "src/main.ts",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build"
  },
  "devDependencies": {
    "vite": "^8.0.8",
    "typescript": "^6.0.3"
  }
}
